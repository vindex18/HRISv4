  $data = DB::select(DB::raw("SELECT MAX(a.datetime) as max, e.first_name, e.last_name, e.is_active, e.pos_title, a.emp_id, t.code, t.description FROM employee e ON e.id = a.emp_id
         LEFT JOIN attendance a ON DATE(a.datetime) = c.datefield AND a.type_id = 1 AND a.emp_id = '$emp_id' 
         WHERE c.datefield BETWEEN '$min_time_in' AND '$max_time_in' AND c.type IN (0, 1, 2, 3, 4) ORDER BY c.datefield DESC"));